configs:
- id: detect-rule-1
  config:
    name:
      name: Appname
      type: environment
    parameters:
      applicationId:
        configId: app-1
        configType: application-web
        property: id
        type: reference
      pattern:
        name: Appname
        type: environment
    template: detect-rule.json
    skip: false
  type:
    api: app-detection-rule
