configs:
- id: Appname
  config:
    name:
      name: Appname
      type: environment
    parameters:
      applicationId:
        configId: Appname
        configType: application-web
        property: id
        type: reference
      pattern:
        name: Appname
        type: environment
    template: lab-detect-rule.json
    skip: false
  type:
    api: app-detection-rule
