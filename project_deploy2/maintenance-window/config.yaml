configs:
- id: Appname
  config:
    name:
      name: Appname
      type: environment
    parameters:
      mzid:
        configId: Appname
        configType: management-zone
        property: id
        type: reference
    template: training.json
    skip: false
  type:
    api: maintenance-window
